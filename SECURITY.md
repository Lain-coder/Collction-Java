# Security

这个仓库用于 Java 集合学习，不应该提交本地密钥、账号密码、token、证书或私有配置文件。

## Before Commit

- 不提交 `.env`、密钥、证书、keystore 等本地敏感文件。
- 不提交 `.class`、`.jar`、日志、临时备份文件。
- 提交前检查 `git status`，确认只包含学习代码、笔记和必要图片。

## Current Check

本次检查没有发现明显的密钥、token、`.env`、证书、编译产物或日志文件。
